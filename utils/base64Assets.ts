// base64Assets.ts
// I have removed the massive string to prevent AI Studio from crashing.
// The app will work, but the specific 'gauge' image might be missing (or use a placeholder).

export const BASE64_SPRITES: Record<string, string> = {
  // We keep the key so the code doesn't break, but make the string empty or a tiny placeholder
  gauge: "", 
};